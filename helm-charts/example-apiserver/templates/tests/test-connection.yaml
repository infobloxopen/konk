apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-apiserver.fullname" . }}-test-connection"
  labels:
    {{- include "example-apiserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-apiserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
