apiVersion: v1
kind: Service
metadata:
  name: {{ include "konk-operator.fullname" . }}
  labels:
    {{- include "konk-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konk-operator.selectorLabels" . | nindent 4 }}
