apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konk-operator.fullname" . }}-test-connection"
  labels:
    {{- include "konk-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konk-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
