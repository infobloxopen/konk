apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konk.fullname" . }}-test-connection"
  labels:
    {{- include "konk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
